const sayHi = (name: string, age: number, gender: string): string => {
  return `Hello ${name}, you are ${age}, your gender is "${gender}"!`;
};

console.log(sayHi("Nayeon", 999, "Prefer not to say"));

export {};
